{
  "name": "basenames-holders",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ basenames-holders",
    "create-local": "graph create --node http://localhost:8020/ basenames-holders",
    "remove-local": "graph remove --node http://localhost:8020/ basenames-holders",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 basenames-holders",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.96.0",
    "@graphprotocol/graph-ts": "0.36.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
